{
  "name": "jobfindingapp",
  "scripts": {
    "start": "docker-compose -f docker-compose.yaml up",
    "stop": "docker-compose -f docker-compose.yaml down",
    "stop:clean": "docker-compose -f docker-compose.dev.yaml down -v"
  }
}